import Billboard from './Billboard';
import SectionOne from './SectionOne';

const Main = () => {
  return (
    <main>
      <Billboard />
      <SectionOne />
    </main>
  );
}

export default Main;
